specVersion: "v1beta"
serviceId: "reliveapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "reliveapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
